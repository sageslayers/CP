#include <bits/stdc++.h>

using namespace std ;

int main () { 
int t ; 

cin >> t ; 
while (t-- )  {
    vector<string> vect(1441);
    string s;  
    int q ; 
    cin >> q;
    int mat[q][2] ; 
    int imp = 1 ;
    char c = 'C' ; 
    for( int i = 0 ; i < q ; i ++ ) {
        cin >> mat[i][0] ; 
        cin >> mat[i][1] ; 
    }
    for( int i = 0 i < n ; i ++ ) { 
		if(mat[i][1]-mat[i][0] == 1 ) {
			int idx  = mat[i][1] ; 
			if(vect[idx] == 'C' && vect[idx+1] == 'C'){
				
			}
		}
	}
    
    
    
    
}

}
