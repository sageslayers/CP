#include <bits/stdc++.h>

using namespace std ; 

int main () { 
	
	int n ,  k  ; 
	
	vector <int> vect ; 
	for ( int i = 0 ; i < n ; i ++ ) {
		int a ; 
		cin >> a;  
		vect.push_back(a) ; 

	}
	int count = 0 ; 
	for ( int i = 0 ; i < n ; i ++ ) {
		int sum = 0 ; 
		for ( int j = i ; j < n ; j ++ ) { 
			
		}
		if ( sum == k ) {
			count ++ ; 
		}
	}
	
}
