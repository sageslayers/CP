#include <bits/stdc++.h>

using namespace std;

void printFemale(int k = 1) {
	for (int i = 0; i < k; ++i) {
		printf("P");
	}
}


void printMale(int k = 1) {
	for (int i = 0; i < k; ++i) {
		printf("L");
	}
}

int main() {
	int t,a,b,k ;  
	scanf("%i",&t);
	while (t--) {
		scandf("%i %i %i",&a,&b,&k);
		int batas = (int) ceil(double) b/k ;
		if ( a < batas -1 || a > 2 (B + 2 ) {
			p("mustahil\n");
		}
		else { 
				
		}
	}
}
